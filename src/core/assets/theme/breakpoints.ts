const mobile = {
  minWidth: 0,
  maxWidth: 739,
};

const tablet = {
  minWidth: 740,
  maxWidth: 979,
};

const desktop = {
  minWidth: 980,
};

export const breakpoints = {
  mobile,
  tablet,
  desktop,
};
